; Minet Test Project

; TODO

; always do errors even if not printing tokens, etc.
; add pos to errors

Test
	.name: "aaron"
	.func: fn()
		this.name: "ed"
		var f: fn()
			console.log(this)
		f()