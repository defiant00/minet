; Minet Test Project

; TODO

; track available variables - string to identifier
; convert head of identifier recursively
; bring back "use x" and "use x as y" so no warnings from existing JS

Proj
	Main: fn()
		alert('hello')