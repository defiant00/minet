; Minet Test Project

use console
	log for console.log

Test
	Func: fn()
		var x, y, z: 1, 2, 3
		var b: x < y < z < 10
		b: x < y = y < z = z > x > 12
		b: x < y = y < z
		b: 0 != x = y = z