; Minet Test Project

use alert

Proj
	getArr: fn()
		ret ["Someone", 32]
	getAnother: fn()
		ret ["Else", 50]
	Main: fn()
		var name, age :: getArr()
		alert(name + " is " + age + " years old.")
		name, age :: getAnother()
		alert(name + " is " + age + " years old.")
