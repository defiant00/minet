; Minet Test Project

use console
	log for console.log

First
	.inst, stat: 1, 2
	.instP
		get
			ret 0
	statP
		get
			ret 0
	.instF: fn()
		ret
	statF: fn()
		ret

First.Sub
	.one, .two: 1, 2