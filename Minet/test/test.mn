; Minet Test Project

use console
	log for console.log

Test
	Func: fn()
		var x, y, z: 1, 2, 3
		if (x < y) = (y < z)
			log('ha')
		if x < y < z < 10
			log('ordered')
		if x = y = z
			log('huh')